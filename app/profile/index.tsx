import { SafeAreaView, Text, View } from "react-native";

const ProfileScreen = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>ProfileScreen</Text>
      </View>
    </SafeAreaView>
  );
};

export default ProfileScreen;
